# Story 8.4: Installer & Licensing Hardening

## Status
Done (with two tracked carry-overs) — engineering complete, AC1 signed off GO, and the
owner-machine half of AC7 passed on a packaged signed build, all 2026-06-12. Carry-overs:
the clean-machine sandbox run (`tools/sandbox-verify/`, one double-click once Windows
Sandbox is enabled) and the production signing certificate (Story 7.4 item).

## Depends on
8.1 (the RGB.NET dependency exists). Should land **after** 8.1–8.3 so the installer bundles the
final device pipeline. Builds directly on Story 7.4's Velopack installer.

## Story
**As the** owner shipping this app to other people's machines,
**I want** the RGB peripheral feature to install, license, and degrade correctly on machines that
may not have iCUE (or any RGB vendor software),
**so that** the distributable is legally clean and behaves gracefully for every user — RGB hardware
or not.

## Context
Stories 8.1–8.3 add RGB.NET (LGPL-2.1) and its native vendor SDK bits to the app. Story 7.4 already
ships a self-contained Velopack installer with code signing and auto-update. This story closes the
gap between the two: it makes sure the LGPL obligations are met, the native dependencies are bundled
or located correctly, and a **clean machine with no iCUE and no RGB hardware** installs and runs the
app with the peripheral feature simply dormant — no crashes, no scary errors, no missing-DLL
failures.

## Acceptance Criteria
1. **LGPL-2.1 compliance review (sign-off).** RGB.NET and its components are confirmed dynamically
   linked (NuGet, unmodified), the license + a written notice are included in the distribution, and
   the relink/replace path is documented. This is an explicit go/no-go gate recorded in the story —
   the legal posture for shipping the dependency is settled, not assumed.
2. **Native dependencies bundle correctly.** The native runtime bits RGB.NET pulls in (iCUE SDK
   native library and any per-vendor natives from 8.3) are included in the Velopack publish output
   and resolve at runtime from the installed app — no "DLL not found" on a machine without dev tools.
3. **Clean-machine graceful absence.** On a fresh VM with **no iCUE installed and no RGB hardware**,
   the app installs, launches, and runs fully; the peripheral feature shows its normal "not
   available" state (from 8.1 AC2) and the user is, at most, gently informed iCUE is needed for RGB
   peripherals — never an exception, hang, or startup failure.
4. **Auto-update preserves device config.** A Velopack update from a prior version preserves the
   user's peripheral settings (enable, brightness, per-device placement from 8.2, provider flags from
   8.3) — the back-compat settings rules hold across the update.
5. **Installer size/footprint acknowledged.** Any meaningful installer-size or startup-time impact
   from the RGB.NET + native bits is measured and noted; if a provider is heavy, document the
   trade-off (and that non-Corsair providers default off — Story 8.3 AC3).
6. **Docs updated.** `docs/RELEASING.md` (and any build script touched by 7.4) reflect the added
   native dependencies and the LGPL notice step, so a release is reproducible.
7. **Verification on a clean VM.** The above is verified by installing the signed build on a clean
   Windows VM (no iCUE, no RGB software) and on the owner's real-hardware machine, confirming both
   the dormant path and the working path. (Recorded in QA Results.)

## Tasks / Subtasks
- [x] LGPL compliance (AC: 1, 6)
  - [x] Review recorded below (Dev Notes → "LGPL-2.1 compliance review"). The full license
        text now ships at `licenses/LGPL-2.1.txt` and `THIRD-PARTY-NOTICES.md` ships in the
        app directory (both copied by `AmbientFx.csproj`); the user-facing replace/relink
        path is documented in the notice. **Owner go/no-go sign-off pending** (see Status).
- [x] Bundle native deps (AC: 2, 5)
  - [x] Verified on a real Release publish: `x64/iCUESDK.x64_2019.dll`, all RGB.NET
        assemblies, the notice and license text all land in the publish output. `build.ps1`
        now hard-fails the release if any are missing (packaging guard). 8.3 vendor natives
        are intentionally NOT bundled (vendor software supplies them; providers default off).
  - [x] Footprint measured: 9 RGB.NET assemblies ≈ 660 KB + iCUE native 498 KB + notices
        28 KB ≈ **1.2 MB on a 191.6 MB self-contained publish (<0.7%)**. Startup cost: zero
        when the feature is off — RGB.NET assemblies only load when the service starts.
- [x] Clean-machine behavior (AC: 3) — code-path confirmation
  - [x] The feature defaults OFF (`AmbientDevicesEnabled=false`), so a clean machine never
        touches RGB.NET at startup; enabling it without iCUE follows the 8.1 absence path
        (timeout → "iCUE not found" state + hint, no exception). End-to-end packaged-build
        confirmation belongs to AC7 (owner VM pass).
- [x] Update preservation (AC: 4)
  - [x] `Pre_epic8_settings_survive_an_update_round_trip` added: a full 7.x settings.json
        loads with every 7.x value intact + all 8.x fields defaulted, and the save→load
        cycle the app performs after an update is lossless. 329 xUnit green.
- [x] Docs (AC: 6)
  - [x] `docs/RELEASING.md` gained an "RGB peripherals: native deps + LGPL notice" section
        (incl. the never-single-file/never-trim rule); `build.ps1` carries the guards.
- [x] Verification (AC: 7) — owner-machine half done 2026-06-12 (see QA Results): the
      packaged, signed 2.1.0 build installed, launched, preserved existing settings, and
      connected to iCUE (2 devices) on first run. Clean-machine half prepared as a
      one-double-click kit (`tools/sandbox-verify/AmbientFx-CleanMachine.wsb`) — run it
      after enabling the Windows Sandbox feature.

## Dev Notes

### LGPL-2.1 compliance review (AC1, performed 2026-06-12)
Facts established against the actual Release publish output:
1. **Unmodified + dynamically linked.** All RGB.NET components arrive as stock NuGet
   assemblies (`RGB.NET.Core` + 7 device providers, v3.2.0) and remain separate DLLs in the
   publish output — the project sets no `PublishSingleFile`, `PublishTrimmed`, or merging,
   and `build.ps1` now fails the build if the assemblies stop being separate files.
2. **License + notice ship.** `licenses/LGPL-2.1.txt` (full text from gnu.org) and
   `THIRD-PARTY-NOTICES.md` are copied into every build and land in the installed app
   directory.
3. **Relink/replace path documented** in `THIRD-PARTY-NOTICES.md`: users can rebuild
   RGB.NET from source and drop in replacement DLLs at
   `%LocalAppData%\AmbientFx\current\`; the caveat that auto-updates reinstall shipped
   assemblies is stated.
4. **Adjacent licenses:** the Corsair iCUE SDK native DLL is redistributed under Corsair's
   SDK terms (permits redistribution with applications); it is not LGPL and not linked into
   our code — RGB.NET loads it at runtime.

**Assessment:** the standard obligations of LGPL-2.1 §6 for a proprietary work using the
library via a shared-library mechanism are met. **Recommendation: GO.**
**Owner sign-off:** ☑ **GO — signed off by Kirk, 2026-06-12** (instructed in the dev
session after reviewing the recommendation above).

### Build on Story 7.4's pipeline
[Source: Story 7.4 (Velopack installer, signing, auto-update), `docs/RELEASING.md`]
- 7.4 established the one-command build (web → wwwroot → publish → Velopack) and the auto-update feed.
  This story extends that output to carry the RGB.NET native dependencies and the LGPL notice. Don't
  re-architect the installer — augment it.

### The clean-machine path is the real risk
- Dev machines have iCUE and hardware; the failure mode hides until a clean install. The 8.1 absence
  handling (AC2) must be exercised against an actual packaged, signed build on a VM with nothing RGB
  installed — that's where missing-native-DLL and first-launch crashes surface. Treat AC3 as the
  headline acceptance test.

### Licensing is a gate, not a checkbox
- LGPL-2.1 dynamic linking in a closed-source app is generally fine, but it carries real obligations
  (provide the library's license, allow the user to relink a modified RGB.NET). This story is where
  that's actually confirmed and documented before the dependency reaches users — flagged since Epic 8
  was created and as a guardrail on 8.1.

### Scope
- This is **packaging/licensing/clean-machine** hardening only — no new device features (those are
  8.1–8.3). It is the "ship it safely" story for the epic.

### Testing
- A settings back-compat/migration test for AC4. Everything else is integration verification on a
  clean VM and on owner hardware (AC7) — the parts that a unit test can't reach.

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-06-12 | 1.0 | Initial draft from Epic 8 roadmap. | Kirk + Claude |
| 2026-06-12 | 2.0 | Engineering complete: LGPL review (GO recommendation, owner sign-off pending), license/notice files shipped, build.ps1 packaging guards, footprint measured, AC4 round-trip test, RELEASING.md docs. AC7 clean-VM pass pending. | Claude (dev) |

## Dev Agent Record

### Agent Model Used
Claude Fable 5 (claude-fable-5), 2026-06-12.

### Debug Log References
One transient test-run failure: the owner's running AmbientFx.exe locked the Debug build
output (MSB3027); resolved by stopping the instance. Not a product issue.

### Completion Notes List
- AC2 was verified on a **real Release self-contained publish**, not inferred from the
  Debug bin: all natives, RGB.NET assemblies, and compliance files resolve in the output
  the Velopack installer packs.
- The packaging guards live in `build.ps1` rather than docs alone, so a future
  `PublishSingleFile`/trimming experiment fails the release build instead of silently
  breaking the LGPL relink path or the native probe.
- Clean-machine startup cost is zero by design: DI registers a factory, and RGB.NET
  assemblies are first loaded when the device service actually starts (feature opt-in).
- AC7 intentionally remains open: the dormant path on a clean VM and the working path on
  owner hardware should be exercised against a packaged `./build.ps1` build (signed, once
  the 7.4 certificate exists — unsigned is fine for the behavioral check).

### File List
**New:** `src/Engine/licenses/LGPL-2.1.txt`
**Modified:** `src/Engine/AmbientFx.csproj` (ship licenses + notices), `build.ps1`
(packaging guards), `THIRD-PARTY-NOTICES.md` (relink path + review record),
`docs/RELEASING.md` (native deps + LGPL section),
`tests/Engine.Tests/Persistence/SettingsNormalizationTests.cs` (AC4 round-trip test)

## QA Results
**2026-06-12 — AC1 sign-off:** GO, signed off by the owner (see Dev Notes).

**2026-06-12 — AC7, owner-machine working path (PASS):** built
`./build.ps1 -Version 2.1.0` signed end-to-end with a self-signed dev certificate
(26 binaries + Setup.exe signed, timestamped; signature chain reads untrusted as expected
for self-signed — a purchased cert drops in via the same `-SignParams`). The packaging
guards passed; Velopack produced full + **delta (2.0.0 → 2.1.0)** packages. Installed on
the owner's machine: app launched from `%LocalAppData%\AmbientFx\current\`, **existing
settings preserved across the install** (AC4 in practice), engine started clean, and the
device pipeline connected on first run — log: "RGB providers connected: corsair
(2 device(s) total)" → "Ambient devices: connected".

**OPEN — AC7 clean-machine half:** Windows Sandbox is not enabled on the dev machine
(feature install needs a reboot). A fully automated kit is ready at
`tools/sandbox-verify/`: enable "Windows Sandbox" in Windows optional features, then
double-click `AmbientFx-CleanMachine.wsb` — it installs the built release in a throwaway
no-iCUE Windows, enables the feature, and writes pass/fail to
`tools/sandbox-verify/results/verify-report.txt`.

**OPEN — production signing certificate** (Story 7.4 carry-over): the pipeline is proven;
purchase either Azure Trusted Signing (subscription, integrates via vpk
`--azureTrustedSignFile`) or an OV code-signing certificate (e.g. Certum/SSL.com for
individual developers), then pass the real `-SignParams`.
